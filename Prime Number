
##### Given a number x,find out if it is a prome number or not,use javascript and find out the differnce between Next prime number after X

const number = parseInt(prompt("Enter a positive number: "));
let count=0
 if (number === 1) {
    console.log("1 is neither prime nor composite number.");
}

for (let i = number;count<2; i++) {
        if (i % 2 == 0) {
            isPrime = false;
            continue;
        }
        else{
            console.log(`${i} number is prime`);
            if (count === 1)
            {
                c=i-number;
                console.log(`${c} is the differnce between ${i} and ${number}`);
            }
            count=++count
            
        }
    } 
